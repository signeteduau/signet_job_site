import { ICompany } from './../types/company-type';

// Demo companies cleared — listings will be populated from the Firebase backend.
const company_data: ICompany[] = [];

export default company_data;
