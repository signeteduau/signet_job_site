"use client";
import React from "react";

// Brand/partner logo strip removed per request (demo company logos).
const PartnersSlider = () => {
  return null;
};

export default PartnersSlider;
